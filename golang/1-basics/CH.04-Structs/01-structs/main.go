// Shows us how structs are set up, this is kind of like OOP classes where we are specifying instance variables
// It should be noted that in GO there actually is no such thing as OOP classes or inheritence



package main

type messageToSend struct {
	phoneNumber int
	message     string
}
