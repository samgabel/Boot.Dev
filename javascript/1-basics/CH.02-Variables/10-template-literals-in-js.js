// You can use a template literal to interpolate, or insert, values into a string template. (think python f-string)

// Anything inside of the ${} is automatically 'cast' into a string
// Cast -> convert from one type to another



const starRating = 10

// don't touch above this line

console.log(`The movie won ${starRating} stars!`)

