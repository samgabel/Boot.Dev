// This is the basic `loop` with index structure...
// `i` is the index that starts at 0
// `i < 100` means that the loop continues over a range that is less than 100
// `i++` defines how the index increments for each subsequent loop



for (let i = 0; i < 100; i++) {
    console.log(i)
}

