// The ternary operator will execute the expression that follows the question mark (?) if `true` and
// will execute the expression that follows the colon (:) if `false`



const bobbyAge = 17
const jillAge = 19

// don't touch above this line

const message = bobbyAge > jillAge ? 'Bobby is older' : 'Jill is older'

// don't touch below this line

console.log(message)

